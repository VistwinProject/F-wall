// ============================================================================
// 面板版面 —— 由編輯模式（鍵盤 e）的「面板」分頁調出來的實際配置
// ----------------------------------------------------------------------------
// 座標系與 appliances.js 相同：viewBox 1920×1080。x/y = 面板左上角。
//
// ⚠ 這裡的值取代了原本「依 panelDir 開在家電旁 + avoidX 自動避讓」的算法。
//    改成絕對座標之後，家電黑塊的座標若有變動（正式量牆後校正），
//    面板【不會】自己跟著跑，要開編輯模式重新對一次。
// ⚠ 面板內的字級 / 行距 / 四邊內距不在這裡，在 config/theme.js 的
//    PANEL_TEXT 與 PANEL_PAD —— 那兩組是九塊面板共用的。
// ============================================================================

// 九個家電的面板：刷到對應的卡片才出現
export const PANEL_LAYOUT = {
  hrv: { x: 115, y: 85, w: 180, h: 220 },
  ac: { x: 1615, y: 105, w: 170, h: 200 },
  dehum: { x: 170, y: 405, w: 170, h: 255 },
  purifier: { x: 1530, y: 380, w: 190, h: 230 },
  sensor: { x: 120, y: 710, w: 215, h: 115 },
  light: { x: 435, y: 800, w: 110, h: 195 },
  socket: { x: 710, y: 820, w: 120, h: 190 },
  curtain: { x: 1195, y: 610, w: 150, h: 180 },
  bathfan: { x: 1530, y: 675, w: 195, h: 120 },
}

// 附屬圖表面板（掛在某台家電旁邊，跟著那台一起出現／消失）。
//
// ⚠ 目前【刻意留空】：面板本身的資訊量已經夠，不再用圖表填版面。
//   原本有三塊 —— socket-chart「用電趨勢」、dehum-chart「每日集水量」、
//   ac-chart「每日耗電」，是在編輯模式的面板分頁刪掉的。
//   要找回來的話它們還在 git 紀錄裡（本次移除前的版本）。
//   同一時間也把 ApplianceNode 裡「資料列排完還有空間就補一張趨勢小圖」拿掉了。
export const CUSTOM_PANELS = []
